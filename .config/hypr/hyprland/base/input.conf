input {
    kb_layout = us,de,eg
    kb_variant =
    kb_model =
    kb_options = caps:escape_shifted_capslock,grp:alt_space_toggle,compose:menu
    kb_rules =

    follow_mouse = 1
    mouse_refocus = false # supposed to fix annoying menu disappearance

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = true
    }
}

gestures {
    # TODO: check why this broke in the latest release
    # workspace_swipe = true
}

